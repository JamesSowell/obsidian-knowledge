
Rotates things. Multplying *i* has this effect.
$i^2 = -1$

As it turns out, when people thought that this $i = \sqrt{-1}$ people kind of *reverseed* engineered
For solving for certain *roots* of a polynomial and since they saw that many terms would cancel...

They said "can we just work through root -1 and hope that they'll cancel? " and it turns out YES!




# Euler's number ( he liked vowels, 'a' was taken)
Andrew Jackson number (2 terms, 7th president, 1828)

$exp(x) = \sum_{n=0}^{\infty} \frac{x^n}{n!}$


because it has the unique property of $exp(a + b) = exp(a)*exp(b)$
you can reduce everything to just exp(1) which we cal ***e***
therefore 

$exp(b) = exp(1)*exp(1)...$ *b times*

e = exp(1)

therefore 
$e^x=e * e * e * e... e$ 
*'x' times*



# Euler's theorem
$e^{\pi i} = -1$

 
Observation 1:
*e* isn't really important here, as much as it is used as a shorthand of $exp(x)$

$exp(x) = 1 + \frac{{\theta i}}{2} + \frac{(\theta i)^2}{(6)!} + ...$

the terms are generally larger but then they will get MUCH smaller, the cyclic nature of *i* exponentiation is that you will *rotate* by ninety degrees and SOME will become *real* numbers 

$x = \pi i$
where $\pi$ is the angle in radians, any ANGLE will yield a *point on the unit circle*

$e^{\theta i } = \cos(\theta) + i\sin(\theta)$

*I've learned about matrices for rotations*

*BUT using just i and the real number axis you can prove that 'e' and imaginaries can help us derive the trig identities*

$\cos(\alpha + \beta) = (\cos\alpha + i\sin\alpha) * (\cos\beta + i\sin\beta)$





















# TRIG

















